// const

function inputReducer(action = [], state) {
  switch (action.type) {
    case 'PUSH_INPUT':
      //
    case 'SELECT_INPUT':
      selected
    default:
      return state;
  }
}

module.exports = inputReducer;
